# Django Settings
# NOTE: This is a lab environment - these values are not sensitive
DJANGO_SECRET_KEY=lab-environment-secret-key-not-for-production-use-only
DEBUG=False

# Database Settings
POSTGRES_DB=sanbox_db
POSTGRES_USER=sanbox_user
POSTGRES_PASSWORD=sanbox_lab_password_2024
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Redis Settings
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# CORS Settings (will work for most internal deployments)
# Add your specific domain if needed
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://localhost,https://127.0.0.1
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://localhost,https://127.0.0.1

# Security Settings
ALLOWED_HOSTS=*

# Celery Settings
CELERY_WORKERS=4

# Application Version
VERSION=latest
